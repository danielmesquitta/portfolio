import React from 'react';

import styles from '../styles';

const { HeroSection, HeroGrid } = styles;

const Home = () => {
  return (
    <HeroSection>
      <HeroGrid>
        <h1>Hello World</h1>
      </HeroGrid>
    </HeroSection>
  );
};

export default Home;
